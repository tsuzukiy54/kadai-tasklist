<h1>id =
<%= @task.id %>
の詳細ページ</h1>



<table class="table table-bordered">
  <tr>
   <th>id</th>
   <td><%= @task.id %></td>
  </tr>

  <tr>
    <th>タスク内容</th>
　　<td><%= @task.content %></td>
  </tr>

  <tr>
    <th>ステータス</th>
　　<td><%= @task.status %></td>
  </tr>

</table>


<%= link_to 'back to list', tasks_path %>
<%= link_to 'edit task', edit_task_path(@task) %>
<%= link_to 'delete task',@task, method: :delete, data: { confirm:'delete OK?'} %>
